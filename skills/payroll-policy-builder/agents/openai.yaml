interface:
  display_name: "Payroll Policy Builder"
  short_description: "Draft and test payroll policy changes"
  default_prompt: "Use $payroll-policy-builder to draft and test this payroll allowance policy."
dependencies:
  tools:
    - type: "mcp"
      value: "prego"
      description: "Prego payroll MCP server"
      transport: "streamable_http"
      url: "https://api.prego.team/mcp"
policy:
  allow_implicit_invocation: true
