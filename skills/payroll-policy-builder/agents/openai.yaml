interface:
  display_name: "Payroll Policy Builder"
  short_description: "Draft, validate, and save payroll items"
  default_prompt: "Use $payroll-policy-builder to draft, validate, and optionally save this payroll allowance policy."
dependencies:
  tools:
    - type: "mcp"
      value: "prego"
      description: "Prego payroll MCP server"
      transport: "streamable_http"
      url: "https://api.prego.team/mcp"
policy:
  allow_implicit_invocation: true
